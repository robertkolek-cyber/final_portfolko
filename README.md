# final_portfolko

Robert Kolek portfolio built with Next.js, Tailwind CSS, and static export for GitHub Pages.

## Scripts

- `npm run dev` starts the local Next.js dev server.
- `npm run build` exports the static site into `out/`.

